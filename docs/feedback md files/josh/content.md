Hello, I need you to analyze my website's code and update the required styling.

I have two main tasks:

Task 1: Fix All Tab Styling
    1.1 Remove the "oval background" on all the tabs that have them. This is likely a border-radius property that needs to be reset to 0 or none.
    1.2 Make the content inside the tabs "hanging" instead. This means I want a hanging indent style: the first line of text should be flush left, and all subsequent lines in the paragraph should be indented.
Task 2: Fix Overview Content Styling
    2.1 Remove its "oval background" just like the tabs (reset border-radius: 0;).
    2.2 Apply the exact same "hanging" indent style to the text content within this overview section.