The following changes are supposed to happen on the overhaul station page:

1. The description text does not follow a consistent format and margin across tabs and lacks visual emphasis, making it harder to read against the background.

Change the color of the description to white with a black shadow and border.
Make the description text wider and aligned to a single, consistent margin used across all tabs (we prefer the wideness and margin of description in Test Bay Tab)
Make the description centered instead of justified
Change the font to Gotham Bold
Apply a black shadow to the description text to improve readability.
Change the color of specific highlighted text as indicated in the wireframe/reference/inspo (COLOR YELLOW or something that matches the gradient)

2. There is no hover design for the current boxes.

Copy the same hover design which is used in Hangar Zones Learning Module 1 and 2 boxes.

3. The fonts used in Laboratory Module and Activity Module boxes are inconsistent with those used in the Test Bay.

Copy and apply the same font style used in the Test Bay modules to Laboratory Module and Activity Module boxes for consistency.
Ensure the font sizes are consistent with Hangar Zones boxes.

4. The label “Preliminary Laboratory Module: Gas Turbine Engine Overhaul” is difficult to read against the background.

Apply a text shadow and make the label dynamic/adaptive to improve readability. Match it to the color of the Overhaul Station 
title.
Reduce the size of the label to make it fit into two lines.

5. The JAJA icon currently used does not match what I wanted

Replace the current JAJA icon with the full-body version exactly as shown in the wireframe. Ensure proper sizing and alignment so it integrates cleanly with the surrounding UI elements.
Link to the JAJA Image: docs/feedback md files/josh/JAJA STANDING W_ VEST.png

6. The Overhaul Station title is a bit small and is not dynamic.

Resize the “Overhaul Station” title to make it slightly larger and more noticeable. 
Apply a dynamic styling similar to the gradient titles on the Homepage to maintain visual consistency and improve emphasis.
Apply a colored shadow that is closely related to its gradient color for emphasis.
Gradient Color Peg: Yellow