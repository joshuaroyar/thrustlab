The following changes are supposed to apply on Hangar Zone page:

1. There is no hover design for the current Learning Module 1 and 2 boxes.

Make sure the box border projects a yellow highlight when hovered, similar to the hover effect used for the researchers’ section on the homepage.

2. The fonts used in Learning Module 1 and 2 boxes are inconsistent with those used in the Test Bay.

Use the same font used in Test Bay module card for Learning Module 1 and 2.

3. The labels for Learning Module 1 and 2 are difficult to read due to insufficient contrast and lack of visual depth.

Implement the same color gradient (instead of the current colors) and font but with a smaller size as the Hangar Zone title for Learning Module 1 and 2 so it fits in 2 lines.

The following changes are site-wide:

4. The space allocated for ALL folder titles is too small, making the text appear too cramped, far to the right/ not centered.

Increase the width of the title area (the blue box make it dynamic and spacious instead of same size) and center-align the titles so they have enough breathing space and appear balanced.

Implement the same in all tabs wherever learning module is used.

5. Some images do not popup in full screen when clicked on for example learning module 1 page 3 last image but the other images pop up on the screen.

Make sure all the images popup in fullscreen when clicked on, just check through that.