The following changes are supposed to happen on the turbofan engine page:

1. The description text does not follow a consistent format and margin across tabs and lacks visual emphasis, making it harder to read against the background.

Change the color of the description to white.
Make the description text wider and aligned to a single, consistent margin used across all tabs (we prefer the wideness and margin of description in Test Bay Tab)
Make the description centered instead of justified
Change the font to Gotham Bold
Apply a black shadow to the description text to improve readability.
Change the color of specific highlighted text as indicated in the wireframe/reference/inspo (COLOR ORANGE or something that matches the gradient)

2. There are unnecessary blank spaces within the content folders (subsonic and bellmouth inlets).

Remove all unwanted excess spacing to create a cleaner and more compact layout.

3. The “Overview” and “Engine Sections” buttons do not visually stand out enough.

Change the font style of both buttons (same to the Tab Button Fonts) to emphasize navigation.

4. Use the pre made section header component for the folder headers, update the colors to match the gradient.

5. Some images do not popup in full screen when clicked on.

Make sure all the images popup in fullscreen when clicked on, just check through that.

6. The Turbofan Engine title is a bit small and is not dynamic.

Resize the “Turbofan Engine” title to make it slightly larger and more noticeable. 
Apply a dynamic styling similar to the gradient titles on the Homepage to maintain visual consistency and improve emphasis.
Apply a colored shadow that is closely related to its gradient color for emphasis.
Gradient Color Peg: Orange

Apply the same effect for learning module 3 but make the text slightly smaller so it fits into two lines.

7. The “Labels Off” button in the 3d model viewer currently does not perform any action when clicked.

Replace the “Labels Off” button with a Mute Audio button instead. Ensure the button clearly reflects its function (e.g., mute/unmute icon and state change) and provides immediate feedback when toggled.
When mute is clicked audio should be muted and when unmuted is clicked audio should immediately resume.